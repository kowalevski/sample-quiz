import React from "react";
import NavLink from "../NavLink";

const Category: React.FC = () => {
  return (
    <div>
      <NavLink to="/quiz" label="↩️ Quiz" />
    </div>
  );
};

export default Category;
